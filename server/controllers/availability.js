const asyncHandler = require("express-async-handler");

// @route GET /availability?day=iso?timeZone=america
// @desc Gets the availability of a user on a given day
// @access Public
exports.logOut = asyncHandler(async (req, res, next) => {});
